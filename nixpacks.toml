# Railway Nixpacks configuration
# Uses requirements.txt for dependencies (auto-detected by nixpacks)

[variables]
PYTHONPATH = "src"

[start]
cmd = "python -m uvicorn junior.main:app --host 0.0.0.0 --port ${PORT:-8000}"
